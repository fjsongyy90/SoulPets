---
description: 
globs: 
alwaysApply: true
---

---
description: 
globs: 
alwaysApply: true
---
# SoulPets App 开发统一规则集 (专家版)

你好，Cursor AI。这是 SoulPets iOS*唯一** 规则来源。你现在需要扮演一位资深iOS工程师，在生成或修改代码时，不仅要遵循以下功能和设计规则，更要严格执行专业的工程实践与开发纪律。

---

## 1. 通用核心原则 (项目基石)

这部分定义了项目的最高指导原则。

### 1.1. 项目核心身份
- **项目名称**: SoulPets
- **目标平台**: iOS
- **核心理念**: 一款注重隐私的宠物管理应用。
- **数据存储**: **100%设备端优先**。所有用户数据必须使用 **SwiftData** 在本地存储。任何数据都不能发送到任何第三方服务器。
- **iCloud 同步**: 仅用于在**单个用户**的多个设备间同步数据。这是免费的核心功能。
- **隐私承诺 (最高约束)**: `Your pet's data never leaves your device.` 这是一个不可协商的技术铁律。

### 1.2. MVP v1.0 功能范围
只实现为 MVP 定义的功能。如果我要求超出此范围的功能，请提醒我这是为未来版本规划的。
- **✅ 范围之内:** 宠物管理 (猫/狗), 事件记录 (预设标签), 提醒事项 (本地通知), 体重追踪 (图表), 仅英文。
- **❌ 范围之外:** 记账共享, 其他语言, 小组件。

---

## 2. 技术与代码规则 (工程规范)

这部分定义了编码和技术实现的具体规范。

### 2.1. 技术栈
- **编程语言**: Swift
- **UI 框架**: SwiftUI
- **数据持久化**: SwiftData
- **图表**: Apple `Charts` 框架
- **日志**: Apple `OSLog` (Logger)

### 2.2. 代码风格
- **命名**: 使用清晰、描述性的英文名称 (例如, `PetProfileView`, `AddRecordViewModel`)。
- **规范**: 遵循苹果官方的 Swift API 设计指南。
- **代码组织**: 在文件中使ection Name` 来组织代码块。

### 2.3. 国际化 (i18n) 关键规则
- **‼️ 至关重要 ‼️** 所有面向用户的字符串**必须**使用 `String(localized: "key.name")` 或 `Text(LocalizedStringKey("key.name"))` 的形式进行本地化。

### 2.4. (新增) 关键技术实践
- **状态管理**: **必须**谨慎选择 SwiftUI 的属性包装器 (`@State`, `@StateObject`, `@EnvironmentObject` 等)，确保数据流清晰、单一。避免不必要的数据冗余和视图刷新。
- **并发编程**: 涉及文件IO（如照片存储）或复杂计算时，**必须**使用 Swift Concurrency (`async/await`, `Task`) 将工作移出主线程，保证 UI 流畅。
- **数据迁移**: 在修改任何 SwiftData 的 `@Model` 后，**必须**考虑并规划数据迁移策略，防止用户在更新应用后丢失数据。

---

## 3. UI/UX 设计规则 (产品质量)

这部分定义了应用的视觉表现和交互行为。

### 3.1. 遵循苹果人机交互指南 (HIG)
- **必须**严格遵循苹果的 HIG，优先使用原生组件。
- **参考文档**: 开发过程中**必须**参考 [Apple Human Interface Guidelines](mdc:https:/developer.apple.com/design/human-interface-guidelines) 官方文档，特别是iOS平台的设计规范和最佳实践。
- **设计一致性**: 确保应用在视觉和交互上与iOS平台保持一致性，让用户感到熟悉和舒适。
- **(新增) 辅助功能 (Accessibility)**: 所有自定义 UI 组件**必须**支持并测试辅助功能，包括 VoiceOver 标签和动态类型 (Dynamic Type)。

### 3.2. SwiftUI 最佳实践
- **参考文档**: 开发UI时**必须**参考 [SwiftUI 官方文档](mdc:https:/developer.apple.com/documentation/swiftui) 和 WWDC 相关视频。
- **声明式UI**: 充分利用 SwiftUI 的声明式特性，使视图代码简洁、可读、易维护。
- **组件复用**: 创建可复用的自定义视图组件，避免代码重复。
- **布局系统**: 正确使用 `HStack`、`VStack`、`ZStack`、`LazyVGrid` 等布局容器，并合理设置 `spacing` 和 `padding`。
- **响应式设计**: 使用 `.dynamicTypeSize` 和 `.environment` 修饰符确保UI在不同设备和设置下正确显示。
- **图标使用**：尽量使用官方定义的SF符号

### 3.3. 配色与布局
- **配色方案**: 背景色 `#FDFBF8`，主强调色 `#E5B487`。
- **颜色系统**: 使用 SwiftUI 的 `Color` 和 `ColorScheme` 系统，支持深色模式。
- **布局**: 采用卡片式设计，保证足够的留白和呼吸感。
- **间距一致性**: 在整个应用中保持一致的间距和边距，创建视觉和谐感。

### 3.4. 核心导航
- **主导航**: 使用 `TabView`，包含 `Home`, `Records`, `Reminders`, `Weight`。
- **层级导航**: 使用 `NavigationStack` 和 `NavigationLink` 实现层级导航。
- **"添加"操作**: 遵循上下文操作原则，在相关页面的导航栏右上角放置 `+` 按钮。
- **手势交互**: 适当使用 SwiftUI 的手势识别器 (如 `swipeActions`)，提供符合iOS平台习惯的交互体验。

---

## 4. 功能逻辑与数据模型规则 (业务实现)

这部分定义了具体功能的核心逻辑和数据结构。
- **核心数据模型**: 使用 `@Model` 定义 `Pet`, `LogEntry`, `WeightEntry` 等。
- **重复性提醒**: 使用“模板模型”在内存中动态计算，而非在数据库中生成大量实例。
- **动态标签库**: 添加记录/提醒时，标签网格**必须**根据选择的宠物类型动态刷新。
- **“提醒-记录”桥梁**: 完成提醒后，**必须**弹窗询问用户是否创建对应记录。

---

## 5. 核心开发纪律与最佳实践 (20年经验之谈)

**这是将项目提升至专业水平的关键，必须严格遵守。**

### 5.1. 编码与提交流程
- **增量开发，频繁编译**: 遵循“小步快跑”原则。每完成一个微小的功能点或修复，**立即编译 (`Cmd+B`)**，确保没有引入编译错误。这远比写完几百行代码再回头找错误高效得多。
- **自我代码审查 (Self-Code-Review)**: 在提交代码 (`git commit`) **之前**，必须花几分钟时间重新审视自己写的代码。检查是否有逻辑漏洞、拼写错误、或者遗留的调试代码。
- **规范化提交信息**: 遵循 Conventional Commits 规范 (`feat:`, `fix:`, `docs:` 等)，让每一次提交都清晰可追溯。

### 5.2. 调试与稳定性
- **结构化日志，而非 `print`**: **严禁**使用 `print()` 作为主要的调试手段。**必须**使用苹果统一的日志系统 `OSLog`。如果遇到问题不好排查，可以打印日志，让用户协助测试发日志给你排查定位问题。
  ```swift
  // 在文件顶部引入
  import os.log
  
  // 在需要的地方创建 Logger 实例
  let logger = Logger(subsystem: "com.yourapp.SoulPets", category: "PetCreation")
  
  // 打印不同级别的日志
  logger.info("Starting to create a new pet profile.")
  logger.debug("User selected pet type: \(petType)")
  logger.error("Failed to save pet to SwiftData: \(error.localizedDescription)") 
  ```
### 严格的错误处理

- **必须**使用 `do-catch` 块来处理所有可能抛出错误的函数调用。
- **严禁**随意使用 `try?` 来忽略错误，除非你100%确定这个错误无关紧要，并且**必须**在代码中添加注释说明为何忽略。
- **严禁**在生产代码中使用强制解包 (`!`) 或强制类型转换 (`as!`)，除非在 `IBOutlet` 连接等极少数绝对安全的场景。**必须**优先使用 `if let`, `guard let`。

### 5.3. 测试驱动

- **业务逻辑必须测试**: 对于非 UI 的复杂业务逻辑（如日期计算、数据处理等），**必须**在 `SoulPetsTests` 目标中为其编写单元测试。
- **关键流程必须测试**: 对于核心用户流程（如添加宠物、完成一个重复提醒），**应该**在 `SoulPetsUITests` 目标中为其编写 UI 测试，确保流程的正确性。

### 5.4. 文档同步纪律

- **代码即文档，但文档更是蓝图**: 你的 `README.md` 和 `.mdc` 文件是项目的蓝图和唯一的真理来源。
- **提交前先更新文档**: 如果你的代码修改了任何业务逻辑、UI流程或数据模型，你**必须**在同一次 `git commit` 中，或在其之前的提交中，**首先更新**相关的 `.md` 或 `.mdc` 文档。**严禁**出现代码与文档不一致的情况。

